
// Loads prototype library.
Object.extend(global, require("prototype"));

/** Top level namespace.
 * @namespace
 */
VM = {};

// Loads all components.
require("./lib/util/IO");
// Loads all components.
require("./lib/application/VM");

VM.io.requireDir(__dirname + "/lib/util/");
VM.io.requireDir(__dirname + "/lib/application/");
VM.io.requireDir(__dirname + "/lib/domain/");
VM.io.requireDir(__dirname + "/lib/integration/");

// Exports loaded components.
for (var property in VM) {
  exports[property] = VM[property];
}

exports.getStaticPath = function () {
  return __dirname + "/lib/application/";
};
